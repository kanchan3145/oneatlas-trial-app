export const routingConfig = {
  intent: {
    primary: { provider: "gemini", model: "gemini-2.0-flash" }
  },
  schema: {
    primary: { provider: "gemini", model: "gemini-2.0-flash" }
  },
  appspec: {
    primary: { provider: "gemini", model: "gemini-2.0-flash" }
  }
};
